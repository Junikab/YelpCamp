<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>New Farm</title>
    </head>
    <body>
        <h1>Farm Details:</h1>

        <div>
            <h3><%= farm.name %></h3>
            <ul>
                <li>City: <%= farm.city %></li>
                <li>Email: <%= farm.email %></li>
            </ul>
            <h4>Products:</h4>
            <ul>
                <% for(let product of farm.products){ %> 
                <li><a href="/products/<%= product._id %> "><%= product.name %></a> - $<%= product.price %> </li>
                <% } %> 
            </ul>
            <a href="/farms/<%= farm._id %>/products/new ">Add product</a>
        </div>
        <div>
            <a href="/farms">All Farms</a>
        </div>
        <div>
            <a href="/farms/<%= farm._id %>/edit ">Edit</a>
            <form action="/farms/<%= farm._id%>?_method=DELETE" method="POST">
                <button>Delete</button>
            </form>
        </div>
    </body>
</html>
